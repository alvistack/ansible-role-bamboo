---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: include default variables
  include_vars: "../vars/main.yml"
  tags: bamboo

- name: include release specific variables
  include_vars: "{{ lookup('first_found', _params) }}"
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "../vars"
  ignore_errors: true
  tags: bamboo

- name: include release specific tasks
  include_tasks: "{{ lookup('first_found', _params) }}"
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "."
  ignore_errors: true
  tags: bamboo

- name: groupadd
  group:
    name: "bamboo"
    system: true
    state: "present"
  tags: bamboo

- name: useradd
  user:
    name: "bamboo"
    group: "bamboo"
    home: "/var/atlassian/application-data/bamboo"
    shell: "/usr/sbin/nologin"
    system: true
    create_home: false
    state: "present"
  tags: bamboo

- name: prepare directories
  file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
    state: "{{ item.state | default('directory') }}"
  loop:
    - { dest: "/etc/init.d" }
    - { dest: "/lib/systemd/system" }
    - { dest: "/opt/atlassian/bamboo", owner: "bamboo", group: "bamboo" }
    - { dest: "/opt/atlassian/bamboo/bin", owner: "bamboo", group: "bamboo" }
    - { dest: "/opt/atlassian/bamboo/conf", owner: "bamboo", group: "bamboo" }
    - { dest: "/var/atlassian/application-data/bamboo", owner: "bamboo", group: "bamboo" }
  tags: bamboo

- name: download archive
  get_url:
    url: "{{ item.url }}"
    dest: "{{ item.dest }}"
    checksum: "{{ item.checksum }}"
  loop:
    - "{{ bamboo_download }}"
    - "{{ mysql_jdbc_download }}"
    - "{{ postgresql_jdbc_download }}"
  register: result
  until: result is succeeded
  tags: bamboo

- name: unarchive package
  shell: |
    set -o pipefail
    cd /opt/atlassian/bamboo
    tar zxf {{ bamboo_download.dest }} --strip-components=1
    cd /opt/atlassian/bamboo/lib
    rm -rf *mysql*.jar
    tar zxf {{ mysql_jdbc_download.dest }} --strip-components=1 --wildcards "mysql*/*mysql*.jar"
    cd /opt/atlassian/bamboo/lib
    rm -rf *postgresql*.jar
    cp {{ postgresql_jdbc_download.dest }} .
    chown -Rf bamboo:bamboo /opt/atlassian/bamboo
  args:
    executable: "/bin/bash"
    creates: "/opt/atlassian/bamboo/bin/start-bamboo.sh"
  notify:
    - bamboo | systemctl daemon-reload
    - bamboo | systemctl restart
  tags: bamboo

- name: copy templates
  template:
    dest: "{{ item.dest }}"
    src: "{{ item.src | default('./templates' + item.dest + '.j2') }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  loop:
    - { dest: "/etc/init.d/bamboo", mode: "0755" }
    - { dest: "/lib/systemd/system/bamboo.service" }
    - { dest: "/opt/atlassian/bamboo/bin/setenv.sh", owner: "bamboo", group: "bamboo", mode: "0755" }
    - { dest: "/opt/atlassian/bamboo/conf/server.xml", owner: "bamboo", group: "bamboo" }
  notify:
    - bamboo | systemctl daemon-reload
    - bamboo | systemctl restart
  tags: bamboo

- name: prepare files
  file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
    state: "{{ item.state | default('file') }}"
  loop:
    - { dest: "/etc/init.d/bamboo", mode: "0755" }
    - { dest: "/lib/systemd/system/bamboo.service" }
    - { dest: "/opt/atlassian/bamboo/bin/setenv.sh", owner: "bamboo", group: "bamboo", mode: "0755" }
    - { dest: "/opt/atlassian/bamboo/conf/server.xml", owner: "bamboo", group: "bamboo" }
  tags: bamboo

- name: flush handlers
  meta: flush_handlers
  tags: bamboo

- name: enable service
  service:
    name: "{{ item.name }}"
    enabled: true
  loop:
    - { name: "bamboo" }
  changed_when: false
  ignore_errors: true
  tags: bamboo
